using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Chronon
{
    public string prose;
    public string[] notes;
    public Chronon(string text,string[] modifiers)
    {
        prose = text;
        notes = modifiers;
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
